#include <stdio.h>
#include "m.h"

int main()
{

  //double a = min(19.68, 18.96);
  //printf("the min is %lf.\n",a);

  //chline('a',10,8);

  //double x=16.8,y=2.0;
  //larger_of(&x,&y);
  //printf("x is %lf, y is %lf\n",x,y);
  
  //double x=7.8,y=16.8,z=3.6;
  //sort_three(&x,&y,&z);
  //printf("x=%lf, y=%lf, z=%lf\n",x,y,z);

  //show_char_index();

  //double n = 2;
  //int p = -8;
  //double pow = power(n, p);
  //double pow = rpower(n, p);
  //printf("%.6lf^%d=%.6lf\n",n,p,pow);


  to_base_n(255);

  return 0;
}
